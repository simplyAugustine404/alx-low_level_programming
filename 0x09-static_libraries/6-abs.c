#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @n: integer whose value is to be computed
 *
 * Return: Absolute value of n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
		return (n);
}
